- convert all datastore callbacks to handle errors differently:
  err = {
      type: <postgres | invalid | ...>
      data: {} // postgres error goes here if type=postgres, description of invalid data if type=invalid, &c.
  }

- implement transactions for DB updates
- implement permissions checking for assignments (can't add assignments if not admin / to someone else's class)
- write blog article about \b and Chinese
- server-side validation of submissions
- allow finer control over submissions (due dates, resubmission counts, etc.)
- allow assignments to be manually graded
- redirect unauthed users from everywhere
- student approval requirement
- scores